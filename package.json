{
  "name": "hello-world",
  "description": "ClaudiaJS hello-world example",
  "version": "1.0.0",
  "private": true,
  "files": [
    "app.js"
  ],
  "scripts": {
    "start": "claudia create --name hello-world --use-s3-bucket node-lambda-bucket-dirkw --region us-east-1 --handler app.handler",
    "test": "claudia test-lambda",
    "deploy": "claudia update"
  },
  "devDependencies": {
    "claudia": "^1.x"
  },
  "repository" :
  { "type" : "git"
  , "url" : "https://github.com/dweibel/nodeLambda.git"
  }
}
